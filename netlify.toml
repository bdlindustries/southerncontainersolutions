# Node version: use NODE_VERSION below. Do NOT commit .nvmrc — browser uploads from Windows add CR (\r)
# and Netlify fails with "Version '20\r' not found".

[build]
  command = "npm install && npm run build"
  publish = "dist"

# Makes sure styling/build tools are always installed (fixes plain white “unstyled” site).
[build.environment]
  NPM_CONFIG_PRODUCTION = "false"
  NODE_VERSION = "20"

[[redirects]]
  from = "/favicon.ico"
  to = "/favicon.svg"
  status = 200

# No catch-all to index.html — this app does not use URL routes; a /* rule can break /assets/* on some setups.
